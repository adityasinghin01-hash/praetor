# PRAETOR runtime + test dependencies.
# Deliberately short: the kernel (resolver, gate, rules) is pure standard library,
# so the security claims can be checked with nothing installed but pytest.

google-genai>=1.0      # the Gemini reader and exception agent
pytest>=8.0            # the invariants, which are the security claims

opentelemetry-sdk>=1.20  # tracing; optional -- without it tracing is a no-op

# The web transport. dashboard/asgi.py serves the same JSON contract as
# dashboard/serve.py, plus paging, live updates and uploads. Both are kept:
# serve.py is standard library only, so `make demo` works on a laptop with
# nothing installed, and a judge cloning this repo can reach the queue without
# a package index. tests/test_asgi.py skips where these are absent.
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9   # uploads; FastAPI refuses to define the route without it

# Only needed to re-render docs/architecture.png from its HTML source:
#   pip install Pillow
#
# Only needed to re-check the hand-rolled logistic regression in eval/train_pathb.py
# against a reference implementation. tests/test_pathb.py skips without it, the same way
# tests/test_trace.py skips without the OpenTelemetry SDK. Path B fits and runs on the
# standard library alone, which is the point:
#   pip install scikit-learn
